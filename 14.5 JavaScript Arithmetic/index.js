// Create the variables to store the value
var dogAge = prompt("How old is your dog?");

/* Add arithmetic operations to get things done and don't forget to add parentheses to all operations that need to be
executed first */
var humanAge = ((dogAge-2)*4) +21;

// And this should be the result, run this code in the browser
alert("Equivalent human age is " + humanAge);